<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="csrf-token" content="<%= csrfToken %>" />
  <title>Todo Manager</title>
  <link rel="stylesheet" href="./css/styles.css" />
  <style>
    body {
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
    }

    .content-box {
      max-width: 400px; /* Adjust the width as needed */
      padding: 20px;
      border: 1px solid #ccc;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    /* Add other necessary styles */
  </style>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <div class="content-box">
    <% include("header.ejs") %>
    <h6 class="m-6">New Here..? <a class="text-green-600" href="/signup">Create a new To-Do account</a></h6>
    <h6 class="m-6">Already have an account..? <a class="text-green-600" href="/login">Sign in here(if an existing user)</a></h6>
  </div>
</body>
</html>
